# leaving-box-bomb-modules

## Run the main.py bridge

From the repository root:

```bash
python main.py
```

## ESP32 helpers

See `esp32/README.md` for MicroPython provisioning (HTTP or BLE) and the
ESP-NOW main controller loop.
